import {tools} from '../lib/tools';export default function ToolGrid({locked,onOpen}){const groups={};tools.forEach(t=>(groups[t.category]??=[]).push(t));return <div className="categories">{Object.entries(groups).map(([cat,list])=><section className="category" key={cat}><div className="categoryHead"><h2>{cat}</h2><small>{list.length} TOOLS</small></div><div className="tools">{list.map(t=><button className={'tool '+(locked?'locked':'')} key={t.id} onClick={()=>onOpen(t)}><span className="lock">{locked?'🔒':'↗'}</span><div className="icon">{t.icon}</div><h3>{t.name}</h3><p>{t.desc}</p></button>)}</div></section>)}</div>}
