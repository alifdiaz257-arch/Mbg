'use client';import {useEffect,useState} from 'react';export default function Header({user,onMenu,onLogin,onLogout}){const [battery,setBattery]=useState('--'),[time,setTime]=useState('');useEffect(()=>{const tick=()=>setTime(new Date().toLocaleTimeString('id-ID'));tick();const t=setInterval(tick,1000);if(navigator.getBattery)navigator.getBattery().then(b=>{const up=()=>setBattery(Math.round(b.level*100)+(b.charging?' ⚡':''));up();b.addEventListener('levelchange',up);b.addEventListener('chargingchange',up)});return()=>clearInterval(t)},[]);return <header className="header shell"><button className="menuBtn" onClick={onMenu}>☰</button><div className="logo">Blue<span>Tools</span></div><div className="headerSpacer"/><div className="status">🔋 {battery} · 🌐 ID · {time}</div>{user?<button className="loginBtn" onClick={onLogout}>Logout</button>:<button className="loginBtn" onClick={onLogin}>Login</button>}</header>}
