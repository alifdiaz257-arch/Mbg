'use client';import {useEffect,useRef,useState} from 'react';export default function QRGenerator(){const [text,setText]=useState('https://example.com');const ref=useRef(null);useEffect(()=>{import('qrcode').then(QR=>QR.default.toCanvas(ref.current,text,{width:240,margin:2}))},[text]);return <><input className="inputArea" value={text} onChange={e=>setText(e.target.value)}/><canvas ref={ref} style={{display:'block',margin:'15px auto',background:'#fff',padding:10,borderRadius:10}}/></>}
