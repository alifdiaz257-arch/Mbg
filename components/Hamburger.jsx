'use client';import DeviceInfo from './DeviceInfo';import PrayerSchedule from './PrayerSchedule';export default function Hamburger({open,close,user,animation,setAnimation,onLogin,onLogout}){if(!open)return null;return <aside className="side"><button className="close" onClick={close}>×</button><h2>Menu</h2><div className="sideRow"><b>Profile</b><p>{user?user.name:'Belum login'}</p>{user?<button className="loginBtn" onClick={onLogout}>Logout</button>:<button className="loginBtn" onClick={onLogin}>Login Google</button>}</div><div className="sideRow toggle"><b>Animasi</b><input type="checkbox" checked={animation} onChange={e=>setAnimation(e.target.checked)}/></div><DeviceInfo/><PrayerSchedule/><div className="sideRow"><b>Tentang</b><p>BlueTools adalah kumpulan utility tools modern untuk kebutuhan sehari-hari dan developer.</p></div></aside>}
