'use client';export default function LoginModal({open,close,onLogin}){if(!open)return null;function login(){const name=prompt('Nama akun Google (demo lokal):','Google User')||'Google User';onLogin({name,email:name.toLowerCase().replace(/\s+/g,'.')+'@gmail.com'})}return <div className="overlay" onClick={e=>e.target.className==='overlay'&&close()}><div className="modal"><button className="close" onClick={close}>×</button><h2>Login Required</h2><p>Login diperlukan untuk membuka semua tools.</p><button className="googleFake" onClick={login}>G&nbsp; Continue with Google</button><small>Untuk Google OAuth asli, gunakan Google Identity Services dan Client ID di environment variable.</small></div></div>}
