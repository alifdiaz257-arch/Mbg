'use client';import QRGenerator from './QRGenerator';import QRScanner from './QRScanner';import {useState} from 'react';export default function ToolModal({tool,close}){const [v,setV]=useState('');if(!tool)return null;let body=<><textarea className="inputArea" value={v} onChange={e=>setV(e.target.value)} placeholder="Masukkan teks..."/><pre className="result">{tool.type==='uppercase'?v.toUpperCase():tool.type==='lowercase'?v.toLowerCase():v}</pre></>;if(tool.type==='qr')body=<QRGenerator/>;if(tool.type==='scan')body=<QRScanner/>;return <div className="overlay" onClick={e=>e.target.className==='overlay'&&close()}><div className="modal"><button className="close" onClick={close}>×</button><h2>{tool.name}</h2><p>{tool.desc}</p>{body}</div></div>}
